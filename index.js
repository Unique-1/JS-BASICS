// console.log('hello world');

// keyword let is used to declared variable in js

// variable
// a variable cannot be researved keywork, it should be meaningful, 
// cannot start a number like (1name), cannot conatin a space or hyphen(-)
// they are case sensitive 
let name = 'codetech';
console.log(name);

// constant 

// keyword const is used to declare constant 
const interestRate = 0.3;
try{
    interestRate = 99;
}catch(err){
    console.log(err)
}

console.log(interestRate);